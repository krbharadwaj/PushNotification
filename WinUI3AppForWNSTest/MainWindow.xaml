<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinUI3AppForWNSTest.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI3AppForWNSTest"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Title="WinUI3AppForWNSTest">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" Margin="0,0,0,10">
            <Button x:Name="InitializePushButton" Content="Initialize Push Notifications" 
                    Click="InitializePushButton_Click" />
        </StackPanel>
        
        <Border Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,10"
                x:Name="StatusBorder" Visibility="Collapsed">
            <StackPanel Spacing="8">
                <TextBlock x:Name="StatusTextBlock" Text="Initializing..." 
                          FontWeight="SemiBold" FontSize="14"/>
                <ScrollViewer MaxHeight="150" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="StepsTextBlock" TextWrapping="Wrap" 
                              FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </ScrollViewer>
            </StackPanel>
        </Border>
        
        <controls:WebView2 Grid.Row="2" x:Name="WebView" 
                          Source="https://example.com" />
    </Grid>
</Window>
