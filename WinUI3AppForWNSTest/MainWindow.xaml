<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinUI3AppForWNSTest.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI3AppForWNSTest"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Dual Push Notification Test - WNS + VAPID">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Dual Push Notifications - WNS + VAPID" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <!-- VAPID Section -->
        <Border Grid.Row="1" Background="#E8F5E8" Padding="15" CornerRadius="8" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="🌐 VAPID Web Push (New Implementation)" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Foreground="DarkGreen"/>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="GenerateKeysButton" Content="🔑 Generate VAPID Key-Pair" 
                            Click="GenerateKeysButton_Click" Margin="0,0,15,0" 
                            Background="DarkGreen" Foreground="White" Padding="15,10" FontSize="13"/>
                    <Button x:Name="SubscribeButton" Content="📡 Subscribe" 
                            Click="SubscribeButton_Click" Margin="0,0,0,0" IsEnabled="False"
                            Background="DarkBlue" Foreground="White" Padding="15,10" FontSize="13"/>
                </StackPanel>
            </StackPanel>
        </Border>
        
        <!-- WNS Section -->
        <Border Grid.Row="2" Background="#E8E8F5" Padding="15" CornerRadius="8" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="📱 Traditional WNS Push (Existing Implementation)" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Foreground="DarkBlue"/>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="InitializeWnsButton" Content="🚀 Initialize WNS" 
                            Click="InitializeWnsButton_Click" Margin="0,0,15,0" 
                            Background="DarkOrange" Foreground="White" Padding="15,10" FontSize="13"/>
                    <Button x:Name="RegisterWnsButton" Content="📋 Register with Server" 
                            Click="RegisterWnsButton_Click" Margin="0,0,0,0" IsEnabled="False"
                            Background="Purple" Foreground="White" Padding="15,10" FontSize="13"/>
                </StackPanel>
            </StackPanel>
        </Border>
        
        <TextBlock Grid.Row="3" Text="Log:" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <ScrollViewer Grid.Row="4" Background="#F5F5F5" Padding="10" CornerRadius="5">
            <TextBlock x:Name="LogTextBlock" Text="Choose your push notification implementation above..." 
                       TextWrapping="Wrap" FontFamily="Consolas" FontSize="12" LineHeight="16"/>
        </ScrollViewer>
    </Grid>
</Window>
